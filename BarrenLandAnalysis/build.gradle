apply plugin: 'java'
apply plugin: 'application'

repositories {
  jcenter()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

run {
  if (project.hasProperty("appArgs")) {
        args Eval.me(appArgs)
    }
}

mainClassName = 'com.target.analyzer.BarrenLandAnalyzer'
